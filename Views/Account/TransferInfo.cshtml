<!DOCTYPE html>
<html lang="tr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Müşteri Aktarımı</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
          .checkbox-group {
        display: flex;
        flex-direction: column;
        gap: 10px;
    }

    .custom-checkbox {
        display: flex;
        align-items: center;
        gap: 10px;
        font-size: 16px;
    }

    .custom-checkbox input[type="checkbox"] {
        width: 20px;
        height: 20px;
        margin: 0;
        cursor: pointer;
        appearance: none;
        border: 2px solid #4e73df;
        border-radius: 5px;
        background-color: #fff;
        transition: background-color 0.3s, border-color 0.3s;
    }

    .custom-checkbox input[type="checkbox"]:checked {
        background-color: #4e73df;
        border-color: #4e73df;
    }

    .custom-checkbox input[type="checkbox"]:checked::after {
        content: '✔';
        font-size: 14px;
        color: white;
        display: block;
        text-align: center;
        line-height: 18px;
    }

    .custom-checkbox label {
        cursor: pointer;
        color: #333;
    }
        body {
            font-family: 'Roboto', sans-serif;
            background: linear-gradient(to right, #1F253A, #2d3a59);
            color: black;
            min-height: 100vh;
        }

        .form-container {
            max-width: 700px;
            margin: 50px auto;
            padding: 30px;
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        .form-title {
            text-align: center;
            margin-bottom: 30px;
            font-weight: bold;
            font-size: 1.8rem;
            color: #2d3a59;
        }

        .form-group label {
            font-weight: 500;
            color: #333;
        }

        .select2-container--default .select2-selection--single {
            height: 50px;
            /* Kutucuğun yüksekliği */
            display: flex;
            align-items: center;
            /* İçerik merkezde hizalanır */
        }

        /* Kutucuk içindeki metni ortalamak için satır yüksekliğini ayarla */
        .select2-container--default .select2-selection--single .select2-selection__rendered {
            line-height: 50px;
            /* Kutucuğun yüksekliğiyle aynı olmalı */
        }

        .btn-primary {
            background-color: #4e73df;
            border-color: #4e73df;
            font-size: 16px;
            font-weight: bold;
        }

        .btn-primary:hover {
            background-color: #375a7f;
        }

        .btn-secondary {
            background-color: #f6c23e;
            border-color: #f6c23e;
            font-size: 16px;
            font-weight: bold;
            color: #fff;
        }

        .btn-secondary:hover {
            background-color: #dda20a;
        }

        .checkbox-group div {
            margin-bottom: 10px;
        }

        #message {
            text-align: center;
        }
    </style>
</head>

<body>
    <div class="container mt-4">
        <h2 class="display-4 text-center">Müşteri Bilgileri Aktarma </h2>
        <hr>
        <div class="form-container">
            <form id="transferInfoForm" method="post">
                @Html.AntiForgeryToken()
                <div class="form-group">
                    <label for="sourceCustomer">Kaynak Müşteri</label>
                    <select class="form-control" id="sourceCustomer" name="sourceCustomerId" required>
                        <option value="">Seçiniz</option>
                        @foreach (var customer in Model.Customers)
                        {
                        <option value="@customer.Id">@customer.Name (@customer.Id)</option>
                        }
                    </select>

                </div>
                <div class="form-group">
                    <label for="targetCustomer">Hedef Müşteri</label>
                    <select class="form-control" id="targetCustomer" name="targetCustomerId" required>
                        <option value="">Seçiniz</option>
                        @foreach (var customer in Model.Customers)
                        {
                        <option value="@customer.Id">@customer.Name (@customer.Id)</option>
                        }
                    </select>
                </div>
                <div class="form-group text-center">
                    <button type="button" class="btn btn-secondary" id="swapCustomers">
                        <i class="fas fa-exchange-alt"></i> Kaynak ve Hedefi Değiştir
                    </button>
                </div>
                <div class="form-group text-center bg-light p-4 rounded shadow-lg">
                    <label class="font-weight-bold h4 text-primary d-block mb-3">
                        📋 Aktarılacak Bilgiler
                    </label>
                    <div class="checkbox-group mt-3">
                        <div class="form-check d-flex align-items-center mb-3">
                            <input type="checkbox" class="form-check-input" id="contacts" name="infoTypes" value="contacts">
                            <label for="contacts" class="form-check-label ml-2">
                                <span class="badge badge-primary p-2"><i class="fas fa-address-book"></i> İrtibat</span>
                            </label>
                        </div>
                        <div class="form-check d-flex align-items-center mb-3">
                            <input type="checkbox" class="form-check-input" id="locations" name="infoTypes" value="locations">
                            <label for="locations" class="form-check-label ml-2">
                                <span class="badge badge-success p-2"><i class="fas fa-map-marker-alt"></i> Lokasyon</span>
                            </label>
                        </div>
                        <div class="form-check d-flex align-items-center mb-3">
                            <input type="checkbox" class="form-check-input" id="records" name="infoTypes" value="records">
                            <label for="records" class="form-check-label ml-2">
                                <span class="badge badge-warning p-2"><i class="fas fa-clipboard-list"></i> Kayıtlar</span>
                            </label>
                        </div>
                        <div class="form-check d-flex align-items-center mb-3">
                            <input type="checkbox" class="form-check-input" id="offers" name="infoTypes" value="offers">
                            <label for="offers" class="form-check-label ml-2">
                                <span class="badge badge-danger p-2"><i class="fas fa-hand-holding-usd"></i> Teklifler</span>
                            </label>
                        </div>
                        <div class="form-check d-flex align-items-center mb-3">
                            <input type="checkbox" class="form-check-input" id="logs" name="infoTypes" value="logs">
                            <label for="logs" class="form-check-label ml-2">
                                <span class="badge badge-info p-2"><i class="fas fa-history"></i> Log Kayıtları</span>
                            </label>
                        </div>
                        <div class="alert alert-warning">
                            <label><i class="fas fa-info-circle"></i> Bu işlem seçilen bilgileri Kaynak müşteriden alır ve hedef müşteriye aktarır </label>
                        </div>
                       
                    </div>
              
                    <div class="alert alert-danger mt-3">
             
                        <label for="deleteSourceCustomer">
                            <input type="checkbox" id="deleteSourceCustomer" name="deleteSourceCustomer" value="true"> Kaynak müşteriyi aktarım sonrası sil
                        </label>      
                    </div>
                    <button type="submit" class="btn btn-primary mt-3 w-50">
                        <i class="fas fa-check-circle"></i> Bilgileri Aktar
                    </button>
                </div>
                
            


                

            </form>
            <div id="message" class="mt-3"></div>
        </div>
    </div>
</body>

</html>

<script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
<!-- Select2 CSS -->
<link href="https://cdnjs.cloudflare.com/ajax/libs/select2/4.1.0-rc.0/css/select2.min.css" rel="stylesheet" />

<!-- jQuery -->
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<!-- SweetAlert -->
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

<!-- Toastify -->
<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css">
<script src="https://cdn.jsdelivr.net/npm/toastify-js"></script>


<!-- Select2 JS -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/select2/4.1.0-rc.0/js/select2.min.js"></script>

<script>
    $(document).ready(function () {
        $(document).ready(function () {
            // SourceCustomer için Select2 başlatma
            $('#sourceCustomer').select2({
                placeholder: "Seçiniz",
                allowClear: true,
                width: '100%' // Select2'nin genişliğini ayarlar
            }).on('select2:open', function () {
                // Açıldığında arama alanına odaklan
                document.querySelector('.select2-container--open .select2-search__field').focus();
            });

            // TargetCustomer için Select2 başlatma
            $('#targetCustomer').select2({
                placeholder: "Seçiniz",
                allowClear: true,
                width: '100%' // Select2'nin genişliğini ayarlar
            }).on('select2:open', function () {
                // Açıldığında arama alanına odaklan
                document.querySelector('.select2-container--open .select2-search__field').focus();
            });
            // Kaynak ve hedef müşterileri değiştir
            $('#swapCustomers').click(function () {
                var sourceCustomer = $('#sourceCustomer').val(); // SourceCustomer seçimi
                var targetCustomer = $('#targetCustomer').val(); // TargetCustomer seçimi

                // Seçimleri değiştir ve Select2'yi güncelle
                $('#sourceCustomer').val(targetCustomer).trigger('change');
                $('#targetCustomer').val(sourceCustomer).trigger('change');
            });

            // Form gönderimi
            $('#transferInfoForm').submit(function (event) {
    event.preventDefault();

    var sourceCustomerId = $('#sourceCustomer').val();
    var targetCustomerId = $('#targetCustomer').val();
    var selectedInfoTypes = [];
    var deleteSource = $('#deleteSourceCustomer').is(':checked'); // Checkbox kontrolü

    $('input[name="infoTypes"]:checked').each(function () {
        selectedInfoTypes.push($(this).val());
    });

    var token = $('input[name="__RequestVerificationToken"]').val();
    var data = {
        SourceCustomerId: sourceCustomerId,
        TargetCustomerId: targetCustomerId,
        InfoTypes: selectedInfoTypes,
        DeleteSourceCustomer: deleteSource // Checkbox verisini gönder
    };

    $.ajax({
        type: "POST",
        url: '@Url.Action("TransferInfo", "Account")',
        data: JSON.stringify(data),
        contentType: "application/json; charset=utf-8",
        headers: {
            "RequestVerificationToken": token
        },
        success: function (response) {
            if (response.success) {
                Toastify({
                    text: response.message,
                    duration: 3000,
                    close: true,
                    gravity: "top",
                    position: "right",
                    backgroundColor: "#4caf50",
                }).showToast();

             
            } else {
                Toastify({
                    text: response.message,
                    duration: 3000,
                    close: true,
                    gravity: "top",
                    position: "right",
                    backgroundColor: "#f44336",
                }).showToast();
            }
        },
        error: function () {
            Toastify({
                text: "Kaynak müşterinin teklifi veya kayıtı mevcut onları aktarmadan silme işlemi yapılamaz !",
                duration: 3000,
                gravity: "top",
                position: "right",
                backgroundColor: "#f44336",
            }).showToast();
        }
    });
});

        });
    });

</script>



</html>